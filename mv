mv-shell integrates keep buffers in sync when
moving files around.  If you enter a 'mv' command on a file that has a buffer opened,
the buffer is also renamed and moved to the location the file is moved to.
